[package]
name = "user"
version = "0.1.0"
edition = "2024"

[dependencies]
fcl_proc_macros = { path = "../fcl_proc_macros" }
# fcl_proc_macros = { path = "../fcl_proc_macros", features = ["idle"] }    # Instrumentation is OFF.
fcl = { path = "../fcl" }

# gag = "1.0"