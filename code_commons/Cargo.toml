[package]
name = "code_commons"
version = "0.1.0"
edition = "2024"

[dependencies]

[features]
common = []
params_logging = [ "common" ]
ret_val_logging = [ "common" ]

# Tmp
# default = [ "ret_val_logging", "ret_val_logging", ]
